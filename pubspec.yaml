name: digital_library
description: My custom library app
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  bloc: ^9.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # dependencies needed
  cached_network_image: ^3.4.1
  dio: ^5.9.0
  firebase_app_check: ^0.4.0+1
  firebase_core: ^4.2.0
  firebase_data_connect: ^0.2.0+2

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^9.0.0

flutter:
  generate: true
  uses-material-design: true
